<%= form_for @property_photo, :html => {:multipart => true} do |f| %>
  <% if @property_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_photo.errors.count, "error") %> prohibited this property_photo from being saved:</h2>

      <ul>
      <% @property_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :property_id %><br />
    <%= f.text_field :property_id %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.file_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
